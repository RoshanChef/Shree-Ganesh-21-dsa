#include <bits/stdc++.h>
using namespace std;

int main()
{

    /*
        and   &
            clear nth bit
            even or odd
            power 2     
            reset first set bit
            bit set or not
            number of 1s

        or    |
            set the bit

        not   ~
            result will be
            n = -(n+1)


        xor   ^
            toggle nth bit
            remove the pairs
            swapping
            1 to n xor
            L to R xor
    */

    return 0;
}
